# Token System

Use the token system across CLI and compatible plugin/skill hosts. Measure before trimming. Select only task-relevant context. Deduplicate repeated tool work. Compact stale observations before they consume the working set. Preserve exact technical literals, failures, negation, ordering, and acceptance criteria. Route focused exploration to isolated contexts when it lowers main-context growth. Verify once after a coherent change. Stop when proof is sufficient.

Loading model: metadata → core directive → detail resources on demand.
