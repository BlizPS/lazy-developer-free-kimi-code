# Lazy Developer

Use the matching skill under `skills/` or `.kiro/skills/`: implementation → lazy-developer; review → lazy-review; failure → lazy-debug; verification → lazy-test.

Keep context focused, inspect the affected path, preserve repository conventions, and verify actual behavior.
Token system: use progressive disclosure and compact working context; preserve exact technical literals.
