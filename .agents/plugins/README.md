# Plugins

This directory contains the repo-local Codex plugin marketplace and the packaged plugin source.

`.agents/skills/` is the portable skill location; `.agents/plugins/` is the Codex plugin packaging area.
