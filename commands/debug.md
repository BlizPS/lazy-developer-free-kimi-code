---
description: Reproduce and fix a failure without speculative rewrites.
---
Use lazy-debug. Reproduce the smallest failing signal, find the first divergence, patch the root cause, and rerun the reproducer plus regression proof. $ARGUMENTS
