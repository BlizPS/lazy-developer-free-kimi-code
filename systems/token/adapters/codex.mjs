export function buildCodexTokenDirective() {
  return 'Codex instructions: keep the working set task-focused, avoid repeated reads/calls, compact stale context, preserve exact technical literals, and stop when proof is sufficient.';
}
