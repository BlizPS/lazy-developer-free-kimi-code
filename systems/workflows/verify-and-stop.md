# Verify and Stop

Translate acceptance into the smallest sufficient proof set. Reuse matching prior results. Run focused checks before broader gates. Distinguish pass, fail, unavailable, and blocked. Do not add unrelated fixes, cleanup, polish, or tests after the requested acceptance proof is complete.
