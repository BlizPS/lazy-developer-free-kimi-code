# Serialization Policy

Persist deterministic, bounded state. Omit functions and unstable runtime objects. Bound large diagnostic payloads. Preserve exact identifiers, paths, errors, and status fields needed for replay or debugging.
