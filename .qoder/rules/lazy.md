# Lazy Developer

Route through the shared root `skills/` tree: implementation/UI → `lazy-developer`; failure → `lazy-debug`; verification → `lazy-test`; review → `lazy-review`.

Keep context focused, inspect before editing, preserve active requirements, and verify actual behavior.
