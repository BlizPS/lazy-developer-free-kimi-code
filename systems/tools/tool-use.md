# Tool Use

Use the cheapest tool that produces the needed evidence. Search and reads are preferred before writes. Reuse successful identical results. Avoid repeating a call when the relevant state has not changed.

Treat high-risk commands as evidence-producing actions: state why they are needed, execute once with the narrowest useful scope, inspect the result, and decide whether another attempt is justified.
