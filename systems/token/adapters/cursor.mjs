export function buildCursorTokenDirective() {
  return 'Cursor rules: apply progressive disclosure, compact active context, deduplicate repeated tool work, preserve exact technical literals, and report only proven results.';
}
