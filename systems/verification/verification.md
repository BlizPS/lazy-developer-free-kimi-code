# Verification

Turn the request into observable acceptance before claiming completion.

Use focused proof first, then broaden only when risk or repository policy requires it. For UI work, verify rendering, narrow-width behavior, states, interaction, and overflow when those behaviors are in scope. Distinguish passed, failed, unavailable, and blocked checks.
