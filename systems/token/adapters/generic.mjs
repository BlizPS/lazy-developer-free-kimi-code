export function buildGenericTokenDirective() {
  return 'Apply the shared token system through progressive disclosure: measure, select, deduplicate, compact, route focused work, verify once, and stop. Do not remove required technical content merely to shorten output.';
}
