export function buildAgentsTokenDirective() {
  return 'Agents instructions: keep context lean, load resources on demand, deduplicate repeated work, compact stale observations, and preserve exact technical literals.';
}
