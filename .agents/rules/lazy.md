# Lazy

Route implementation → `lazy-developer`; failure → `lazy-debug`; verification → `lazy-test`; review → `lazy-review`. Inspect first, preserve active requirements/local conventions, use high-information actions, and prove actual results.
