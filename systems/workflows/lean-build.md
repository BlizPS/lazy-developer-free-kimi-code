# Lean Build

Use for new behavior, integrations, or changes with overbuilding risk.

Derive observable acceptance and explicit non-goals. Trace the owning layers. Deliver one coherent end-to-end path using existing seams. Add dependencies, services, configuration, migrations, or abstractions only when acceptance or lifecycle design requires them. Run focused proof and stop when acceptance passes.
