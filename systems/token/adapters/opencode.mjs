export function buildOpenCodeTokenDirective() {
  return 'OpenCode instructions: use the shared token system for progressive loading, focused context, bounded observations, deduplication, and result-first output.';
}
