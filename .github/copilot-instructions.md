Match one skill to the task: implementation → `skills/lazy-developer/`; failure → `skills/lazy-debug/`; verification → `skills/lazy-test/`; review/audit → `skills/lazy-review/`.

Inspect first. Search only when current/external facts can change the result. Preserve active requirements and local conventions; prefer small complete repo-native changes. Verify real coverage; never claim an unrun check passed.
