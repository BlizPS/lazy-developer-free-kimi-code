# Context Compaction

Compact by meaning, not by arbitrary truncation.

Pin explicit constraints, exact paths and identifiers, acceptance criteria, failure evidence, current decisions, and verification results. Collapse repeated tool output and stale exploration. Keep unresolved risks until closed.

When context is tight, prefer the current task, active code path, latest failure, latest decision, and proof over historical chatter.
