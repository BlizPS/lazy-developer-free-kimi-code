export function buildSkillTokenDirective() {
  return 'Use progressive disclosure. Keep discovery metadata tiny. Load SKILL.md only on match. Read references, scripts, and assets only when required. Avoid transcript-sized examples and repeated policy.';
}
