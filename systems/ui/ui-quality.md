# UI Quality Contract

Treat UI as a product system with hierarchy, behavior, content density, and responsive rules.

Keep the existing identity unless redesign is requested. Research named references rather than guessing. Extract principles instead of cloning pixels. Prefer semantic structure and real states over decorative complexity. Reject generic card grids, unnecessary gradients/glass, oversized heroes, fake controls, invented content, and arbitrary visual noise unless the product context supports them.
